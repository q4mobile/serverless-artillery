# Load Test Plan — PSTN Dial-out (Production)

**Execution window:** Friday **2026-04-17**, **03:00–03:30** **America/Toronto** (Toronto / Eastern Time, IANA `America/Toronto`)  
**System under test:** Conference participant dial-out via AWS Chime SDK Voice  
**Prepared by:** Engineering  
**Target environment:** Production

---

## Event schedule

| Item | Value |
|---|---|
| **Planned execution date** | **2026-04-17** (Friday) |
| **Planned start time** | **03:00** |
| **Time zone** | **America/Toronto** (Eastern Time; observes DST — mid-April is **EDT**, UTC−4) |
| **Expected end time** (active test + immediate report) | **03:30** |
| **Backup / rain date** | _Optional: next low-traffic slot, America/Toronto_ |
| **Go / No-Go decision** | **Emilia Siqueira, Senior Product Manager — by 2026-04-16 EOD America/Toronto** |
| **On-call during window** | **Serhii Zvinskyi, Slack — 02:30–04:00 America/Toronto** |

---

## 1. Objective

Validate dial-out at scale — connects, IVR (meeting ID + PIN), `CONNECTED`, hand-raise toggle, no critical errors in Chime SMA / Lambdas / DynamoDB path used by the test.

---

## 2. Scope

**In scope:** Outbound PSTN dial-out, Dynamo-gated DTMF, hand raise in scenario, `npm run report` on `data/run-state.ndjson`.

**Out of scope:** Inbound PSTN load (see program-level docs).

---

## 3. Test Environment

| Component | Production value |
|---|---|
| AWS Region | `us-east-1` |
| Chime SMA ID (test) | `<TBD>` |
| Caller ID | `+13656574084` (E.164) |
| Called number | `+15855429983` (E.164) |
| DynamoDB table | `events-streaming-serverless-conference-participants-prod` |
| EP API (registration) | `https://attendees.events.q4inc.com/rest/v1` |
| Production SMA guard  | `PRODUCTION_SMA_ID=<PRODUCTION_SMA_ID>` |

---

## 4. Load profile for this window

**Fill one row only** — must match `data/analysts-payload.csv` row count and `tests/dial-out-payload-example.yml` `phases`.

| Choice | Meetings | Participants | Artillery (example) | Notes |
|---|---|---|---|---|
| **A — Fits 30 min** | 1 (typical) | **20** | `duration: 120`, `arrivalCount: 20` | Matches current default smoke in repo |
| **B — Full** | 4 | **900** | `duration: 300`, `arrivalCount: 900` | **Expect > 30 min** total; adjust schedule |

---

## 5. Entry criteria (Go / No-Go)

Use the checklist in [`load-test-plan-template.md`](./load-test-plan-template.md) §5. Minimum for tomorrow:

- [x] `data/analysts-payload.csv` regenerated **same day**; row count = sum of `arrivalCount` (and matches plan file).
- [x] `tests/dial-out-payload-example.yml` phases aligned with that row count.
- [x] `PRODUCTION_SMA_ID` set; test SMA id verified.
- [x] Smoke passed in non-prod within 48 h (template criterion).
- [x] On-call assigned **02:30–04:00** if large run or first prod dial-out at scale.

---

## 6. Exit criteria (Pass / Fail)

Same thresholds as template §6 (e.g. ≥ 95% `CONNECTED`, Artillery `maxErrorRate: 0`, etc.).

---

## 7. Execution steps (timed to 03:00 start)

| Clock (America/Toronto) | Step |
|---|---|
| **02:30** | Pre-test verification (Lambda resource policy, env vars, CSV row count). Template §7.2. |
| **02:45** | Optional **smoke** (20) if not already green today — **only** if it does not collide with 03:00 prod window policy. |
| **03:00** | **Start Artillery** — template §7.3 commands with **prod** table and URLs. |
| **03:00–03:30** | Active monitoring (template §8). |
| **Immediately after Artillery** | `npm run report` — template §7.4. |
| **03:30–04:00** | Results review; if metrics were borderline, **extend watch** per table above. |

### 7.3 — Command (reference)

```bash
cd ep-load-test

LOAD_TEST_SMA_ID="<test-sma-id>" \
LOAD_TEST_FROM_PHONE="+13656574084" \
LOAD_TEST_TO_PHONE="+15855429983" \
DIALOUT_PARTICIPANTS_TABLE_NAME="events-streaming-serverless-conference-participants-prod" \
PRODUCTION_SMA_ID="<PRODUCTION_SMA_ID>" \
npx artillery run tests/dial-out-payload-example.yml
```

---

## 8. Monitoring, rollback, artefacts

Identical to template §§8–9–12 ([`load-test-plan-template.md`](./load-test-plan-template.md)).

---

## 9. Sign-off

| Role | Name | Date |
|---|---|---|
| Execution lead | Serhii Zvinskyi | 2026-04-17 |
| Engineering manager (if required) | | |
