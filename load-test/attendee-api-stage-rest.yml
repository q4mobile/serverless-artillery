config:
  target: "https://attendees.stage.events.q4inc.com/rest/v1"
  ensure:
    max: 5000 # fail if max response time exceeds 5000ms
    maxErrorRate: 0 # fail if error rate exceeds 0%
  phases:
    - duration: 60           # 5 min
      arrivalRate: 166        # 166 attendee's per second ~ 10k per minute
      name: "Perform Attendee load test 300-166-179891951-auth"
scenarios:
  - name: "Perform Attendee http journey to console"
    flow:
     